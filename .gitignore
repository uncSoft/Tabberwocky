# Build artifacts
.build/
*.app
*.zip
DerivedData/
.build-test/
.swiftpm/

# Demo videos — GitHub-hosted via user-attachments (drag into an issue/comment),
# kept on disk locally. The inline GIF (assets/*.gif) stays tracked.
assets/*.mp4

# macOS
.DS_Store

# Xcode
xcuserdata/
*.xcuserstate

# Local-only Xcode project for the example (canonical build is SwiftPM / build.sh)
Examples/DocumentTabsShowcase/*.xcodeproj
